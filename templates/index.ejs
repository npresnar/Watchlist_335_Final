<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movie Watchlist Organizer</title>
</head>
<body>
    <h1>Movie Watchlist Organizer</h1>
    
    <!-- Link to Watchlist Page -->
    <a href="/watchlist">Go to Watchlist</a>

    <h2>Trending Movies</h2>
    <ul>
        <% movies.forEach(movie => { %>
            <li><a href="/movie/<%= movie.id %>"><%= movie.title %></a></li>
        <% }); %>
    </ul>

    <h2>Searchable List of Movies</h2>
    <form action="/search" method="POST">
        <label for="search">Search:</label>
        <input type="text" id="search" name="query" placeholder="Search for a movie...">
        <button type="submit">Search</button>
    </form>
    
</body>
</html>
